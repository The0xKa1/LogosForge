# 接口文档

## POST /api/textbooks/upload

上传一个或多个教材文件。

响应：

```json
{
  "textbooks": [
    {
      "textbook_id": "book_xxx",
      "filename": "生理学.pdf",
      "file_format": "pdf",
      "status": "uploaded"
    }
  ]
}
```

## POST /api/textbooks/parse

解析教材并返回章节结构。

请求：

```json
{ "textbook_ids": ["book_xxx"] }
```

## POST /api/graph/build

构建单本或全部已解析教材的知识图谱。

请求：

```json
{ "textbook_id": "book_xxx" }
```

## POST /api/graph/merge

跨教材对齐，输出整合图谱和决策。

响应包含：

```json
{
  "decisions": [
    {
      "action": "merge",
      "affected_nodes": ["node_a", "node_b"],
      "reason": "两本教材覆盖同一概念",
      "confidence": 0.86
    }
  ],
  "compression_ratio": 0.27
}
```

## POST /api/rag/query

请求：

```json
{ "question": "炎症是什么？" }
```

响应：

```json
{
  "answer": "基于当前教材证据...",
  "citations": [
    {
      "textbook": "病理学",
      "chapter": "第四章 炎症",
      "page": 78,
      "relevance_score": 0.92
    }
  ],
  "source_chunks": ["炎症是..."]
}
```

实现说明：

- 服务读取 `backend/.env` 中的 `LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL`。
- `/api/rag/index` 会将当前已解析教材 chunk 写入本项目 `backend/chroma_db`。
- 检索优先使用 ChromaDB + `EMBEDDING_MODEL`，默认模型为 `BAAI/bge-large-zh-v1.5`。
- 若 Chroma 或 embedding 模型不可用，自动回退到本地词项检索。
- `/api/rag/status` 会返回当前 retriever 状态，并显示外部 `/Users/zhangjinkai/textbooks/medical-rag/chroma_db` 的可用 chunk 数。
- 接口兼容 OpenAI Chat Completions 格式。
- 检索 top-5 chunk 后，将教材名、章节、页码和正文注入 prompt。
- 模型必须只基于上下文回答，并使用 `[教材名称, 章节, 第 X 页]` 格式引用。
- 如果缺少 key、依赖或模型调用失败，系统自动回退到本地检索摘要。

## POST /api/teacher/chat

请求：

```json
{
  "message": "我觉得免疫应答不应该被删除，请保留",
  "decision_id": "merge_xxx"
}
```

## POST /api/report/generate

生成并写入 `report/整合报告.md`。
